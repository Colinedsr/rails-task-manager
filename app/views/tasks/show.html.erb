<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<%= link_to 'Delete', task_path, method: :delete, data: { confirm: 'Are you sure you want to delete this restaurant?' } %>


<%= link_to 'Edit task', edit_task_path %>

<%= link_to 'List of tasks', tasks_path %>
